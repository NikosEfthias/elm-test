module Login exposing (main)

import Bootstrap exposing (CDN, Grid)
import Html exposing (Html, div)


type alias Model =
    ()


type alias Msg =
    ()


view : Model -> Html Msg
view model =
    div [] []


main =
    3
